<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Page</title>
  <link href="/css/style.css" rel="stylesheet">
</head>
<body>
  <header>
    <nav>
   

      <a href="/users/login">login</a>
      <a href="/users/signup">signup</a>
      <a href="products/add">add product</a>
    </nav>
  </header>
  <main>
   
    <div class="product-details">
      <% for (let product of allProduct) { %>
        <div class="product-image">   
          <% for (let image of allimage) { %>
            <% if (image._id.toString() === product.image.toString()) { %>
              <img src="<%= image.filePath %>"> 
            <% } %>
          <% } %>
        </div>
      <div class="product-info">
        <h2><%= product.name %></h2>
        <p><%= product.category %></p>
        <p><%= product.shortDescribe %></p>
        <p>&#36; <%= product.price %> </p><label for="quantity">Quantity:</label>
        <input type="number" id="quantity" name="quantity" min="1" step="1">
        <form class="login100-form validate-form flex-sb flex-w" action="/carts" method="post" id="addTocart">
        <button type="submit">Add to Cart</button>
      </div>
    </form>
      <% } %>
    </div>

  </main>
  
<script>
  const form = document.getElementById('addTocart');

  form.addEventListener('submit', async (event) => {
  
    const Data='';

    const formData = new FormData(form);
   
       Data = {
        userId: userId,
        productId: product._id,
        amount:formData.get('quantity'),
    };
     console.log(product._id);
   
const response = await fetch('/carts', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(Data),
    });
    const data = await response.json();
    window.location.href = `users/login`;
  
  });
</script>
</body>
</html>
